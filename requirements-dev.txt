# Testing dependencies
pytest==7.4.3
pytest-cov==7.0.0
pytest-mock==3.12.0
moto[ses,dynamodb]>=5.0.0  # AWS service mocking (v5 for mock_aws decorator)
boto3-stubs[ses,dynamodb]==1.34.0

# Linting and formatting
pylint==3.0.3
black==23.12.1
flake8==7.3.0
mypy==1.19.1

# Type hints
types-boto3==1.42.44

# Security scanning
bandit[toml]>=1.7.5
safety>=3.0.0  # v3+ to fix packaging dependency conflict with black

# Integration testing
requests==2.32.5
